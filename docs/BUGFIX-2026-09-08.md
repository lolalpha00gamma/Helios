# Bugfix-Pass Helios 1.6.45 — 2026-09-08 (Pass 9)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 9 — gelandet

1. **HMM pinchHeld Call-Site tot.** `step` Default `false` — Gate Held, HMM sprang auf Faust/unknown → Klick-Abbruch bei 8 fps. Fix: `hmm.step(..., pinchHeld: pinchState.closed)`.
2. **`let dt` Shadow.** Zweite Uhr `now - lastNow` ungeklemmt, Dropout-Sprung in palmWidthEMA. Fix: `dtPalm = dt` (sampleDt).
3. **Format-Retry tot.** `cameraFormatRenegotiateRetry` nur Tests. `already` 8 s blockierte die Leiter. Fix: Retry nach 3 s ODER first-Drop.
4. PoseHMM Boost + Stay-Tau 110 ms bleibt.

## Offen

DepthCapture Stub. Zwei TCC mit Aegis (CameraBroker). uniqueID sticky.

Loop: drei Folge-Prüfungen auf HandTracker.analyze ohne neue *Need(dt).
