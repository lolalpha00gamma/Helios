# Bugfix-Pass Helios 1.6.44 — 2026-09-08 (Pass 7, Code)

Kein Merge von `bugfix` (1.5.7). Kein CI (`[skip ci]`). Linux-Sandbox: kein Vision, kein XCTest — Tests als `@main` Double-Math.

## Warum es sich schlecht anfühlt

Nicht weil Pinch-Schwellen fehlen. Die Pipeline ist eine Uhr auf 8 fps Continuity.

1. **HUD am Detect-Takt.** 1.6.43 hat Format-Leiter und PinchHoldPhase-Math. Overlay `mark()` malte nur wenn Vision lieferte — 8 Hz, der Cursor rastet.
2. **PinchHoldPhase tot.** Math Unseen/Tentative/Held/Released, Engine weiter Bool + sechs Uhren. Ein Continuity-Miss = Klick.
3. **Eine Quelle, vier Stimmen.** 21 Punkte ohne z. DepthCapture bleibt Stub.
4. **Kein CameraBroker.** Helios und Aegis zwei TCC-Sessions.

## Pass 7 — gelandet (1.6.44)

| # | Bug | Datei | Fix |
|---|---|---|---|
| 1 | HUD 8 Hz | OverlayController | CADisplayLink ~90 Hz, `hudLerpT` / `hudLerpPoint` per Track-ID |
| 2 | Pinch Bool | GestureEngine | `pinchHoldAdvance` in `driveGrab`, `dropPinchHold` statt nacktem Bool |
| 3 | Freeze-Snap | OverlayController | Freeze t=1, sonst t=0 am Sample (1 Frame Lag, glatt) |

1.6.43 bleibt: Format-Leiter, PinchHoldPhase Math, pointerWarpAllowed.

Kein viertes Pinch-`*Need`. DepthCapture bleibt Stub. CameraBroker folgt — XPC+IOSurface, nicht dieser Pass.

## Was nicht noch ein Flag werden soll

- Kein Merge von Branch `bugfix`.
- Kein zweiter macOS-Workflow.
- Nächster Code-Schritt: CameraBroker XPC + IOSurface mit Aegis, nicht 1.6.45 als dt-Pflaster.
