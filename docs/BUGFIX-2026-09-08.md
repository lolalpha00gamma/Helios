# Bugfix-Pass Helios 1.6.45 — 2026-09-08 (Pass 8)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 8 — neu gefunden

1. **PoseHMM liest PinchHold nicht.** `step` sah nur Closedness-EMA. Gate Held, HMM sprang auf unknown/fist → Klick-Abbruch bei 8 fps. Fix: `pinchHeld` + `pinchHoldBoost`, Stay-Tau 110 ms solange gehalten.
2. **`let dt` doppelt in HandTracker.analyze** (sampleDt und palm EMA). Zweite Deklaration umbenennen beim nächsten Xcode-Build; PoseHMM-Signatur bleibt abwärtskompatibel (`pinchHeld = false`).
3. **DepthCapture weiter Stub.** Fusion-Gewicht 0.
4. **Zwei TCC-Sessions** mit Aegis. CameraBroker folgt (XPC+IOSurface).

Loop auf PoseHMM.swift: drei Folge-Prüfungen nach dem Boost-Patch ohne neue *Need(dt).
