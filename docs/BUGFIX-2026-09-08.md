# Bugfix-Pass Helios 1.6.47 — 2026-09-08 (Pass 10b)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 10b — gelandet

1. **start() hält Leiter.** 1.6.46 setzte lastFormatHeight=1080 und lastDeviceUniqueID="" — Continuity-Reconnect kletterte wieder 1080p@8.
2. **lastFormatHeightResets sticky.** uniqueID-Wechsel allein dumpte Höhe. Jetzt Name+Role, Mac nie.
3. **Mac nie sticky.** FaceTime-Name ist nicht Continuity.

## Offen

DepthCapture Stub. Zwei TCC mit Aegis (CameraBroker). Sleep/Wake Camera-Recovery.

Loop: drei Folge-Prüfungen — sticky iPhone hält 720; Mac-ID reset; start() ohne 1080-Wipe. Kein neues *Need(dt).

# Bugfix-Pass Helios 1.6.46 — 2026-09-08 (Pass 10)


Kein Merge von `bugfix`. `[skip ci]`.

## Pass 10 — gelandet

1. **uniqueID sticky.** Continuity flackert uniqueID, Homographie+Recenter bei jedem Reconnect. `cameraIDSticky` / `cameraIDHomographyResets`: gleicher Name (ohne ` · Tiefe`) hält Map. `SpaceMap.retarget` schreibt Homographie unter neue ID. Erster Frame (prev leer) kein Reset.
2. **PTS-Sprung = Freeze.** `accept()` nahm Roh-PTS als `arrived`. Continuity-Uhr-Reset = multi-second Dropout, Hands tot. `ptsJumpIsFreeze` (d ≤ 0 oder > 0,50 s) + `ptsWallStamp` auf Wall.
3. **lastFormatHeight Reset.** Leiter klebte 1080p der alten Cam nach uniqueID-Wechsel. `lastFormatHeightResets` setzt Höhe auf 1080.
4. **preferredDevice Name-Reconnect.** uniqueID tot nach Reconnect → Built-in. `cameraPreferredID` matcht ID, sonst `cameraNameBare`. `helios.cameraName` persistiert.
5. **Kalman-Palme in HandTracker während Freeze.** Leere Observations standen 90 ms. `freezeKalmanPredict` mit `lastVel` (Palm-Delta live) schiebt lastHands.

## Offen

DepthCapture Stub. Zwei TCC mit Aegis (CameraBroker). Sleep/Wake Camera-Recovery.

Loop: drei Folge-Prüfungen — Continuity Reconnect ohne Recenter; PTS-Sprung hält Palm; Leiter nach Cam-Wechsel neu.
