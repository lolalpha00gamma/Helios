# Bugfix-Pass Helios 1.6.58 — 2026-09-08 (Pass 21)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 21 — gelandet

1. **Predict-Clutch.** `pointerPredictArmed` / Cap 0 bei Deadman oder Zwei-Hand. One-Euro-Deriv coastete 48 pt trotz dx=0.
2. **USB-C 30-fps Promote.** `cameraLockFpsPromote` / `cameraFormatPromoteReady` nur nach gemessenen ≥ 22. Osmo claimed 30 bleibt 8.
3. **Fling-Cap × Screen-Höhe.** `flingTeleport` 42 % Höhe. Continuity-Dropout war Dock/Minimize.
4. **Per-App Scroll-Gain.** Safari 1,35×, Xcode 0,55×. `scrollGainFor` in two-pinch Ticks.

## Offen

DepthCapture Stub. CameraBroker XPC. Overlay-Metal. MediaPipe. VNTrackObjectRequest.

Loop: drei Folge-Prüfungen — Clutch ohne Predict-Coast; USB-C ≥ 22 → 30; Dropout kein Fling. Kein neues *Need(dt).

# Bugfix-Pass Helios 1.6.57 — 2026-09-08 (Pass 20)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 20 — gelandet

1. **Pointer 1-Frame Predict.** `pointerPredict` / `pointerPredictPoint` aus One-Euro-Vel. 8 Hz Sample laggt 125 ms — Predict cap 48 pt.
2. **Two-Hand Clutch.** `twoHandClutch` zweite Palme ohne Zwei-Pinch friert Actor + Klick.
3. **Stage-Manager Space-Clamp.** `stageManagerClamp` auf `visibleFrame`. Cursor nicht in Dock/Menü/Stage-Strip.
4. **Hand-Box IoU persist.** `handBoxTrackKeeps` / `handBoxTrackStep`. Slot-ID zwischen 8-Hz-Detect.

## Offen

DepthCapture Stub. CameraBroker XPC. Overlay-Metal. MediaPipe. USB-C 30-fps Promote.

Loop: drei Folge-Prüfungen — 8 Hz Cursor nicht 1 Frame hinterher; zweite Hand kein Klick; Stage-Strip hält. Kein neues *Need(dt).

# Bugfix-Pass Helios 1.6.56 — 2026-09-08 (Pass 19)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 19 — gelandet

1. **One-Euro Filter.** `oneEuroMinCutoff` × Jitter, Filter auf Cursor-Punkt. 8 Hz Highpass allein ließ Teleport.
2. **Continuity 24-fps Kaltstart.** `cameraFormatColdStartBias`: 720@24 schlägt claimed 1080@30 ohne Messung. Cover lockte `minFrameDuration` = Max-FPS → 8 Hz. Jetzt `cameraLockDuration` 24.
3. **Bezel-Hop Hysterese 80 pt.** `bezelHopAllows`. Sidecar-Rand zitterte den Cursor rüber.
4. **Palm-Deadman 2 s.** Stillstand clutcht, blockt Klick. Continuity-Drift war Click.

## Offen

DepthCapture Stub. CameraBroker XPC. Overlay-Metal. MediaPipe. VNTrack Hand-Box.

Loop: drei Folge-Prüfungen — iPhone-Start Chip 720@24 nicht 1080@8; Sidecar-Rand hält 80 pt; 2 s still = kein Klick. Kein neues *Need(dt).

# Bugfix-Pass Helios 1.6.55 — 2026-09-08 (Pass 18)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 18 — gelandet

1. **Adaptive Gain aus Jitter-RMS.** `pointerGainAdaptive` / `jitterRms`. 8 Hz Continuity-Rauschen dämpft, Intent bleibt.
2. **HUD aus Screen-Capture.** `sharingType = .none`. Nicht in Screenshot/Aufnahme.
3. **Window-Edge Resistance.** Gain 0,35 in 56 pt Band. Hartes Clamp tot.
4. **Multi-Display Warp.** `stepCursor` auf aktuellem Schirm, nicht Union. Lücke clamp, Hop nur auf echten Schirm.

## Offen

DepthCapture Stub. CameraBroker XPC. Overlay-Metal. MediaPipe. VNTrack Hand-Box.

Loop: drei Folge-Prüfungen — Continuity-Jitter ohne Teleport; HUD unsichtbar in Screenshot; Sidecar-Lücke hält den Cursor. Kein neues *Need(dt).

# Bugfix-Pass Helios 1.6.54 — 2026-09-08 (Pass 17)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 17 — gelandet

1. **Per-Display backingScaleFactor.** `ScreenGeometry.backingScale(quartz:)` statt `NSScreen.main`. 5K ≠ Sidecar 1×.
2. **Deadzone × Scale.** `deadzoneScaled` — UV-Jitter leckt sonst als CGEvent-Echo.
3. **Two-pinch Scroll-Momentum.** Loslassen füllt `scrollCoast` aus letzten Ticks. Ein-Finger-Inertia bleibt.
4. **Homographie-Cache keyed by rotation.** `spaceMapRotationKey` 0/90/180/270. Nudge ohne Store-Wipe.
5. **Click-Energy.** Fest+still klickt früher. Wackel-Pinzette blockt trotz Dauer.

## Offen

DepthCapture Stub. CameraBroker XPC. Overlay-Metal. MediaPipe. VNTrack Hand-Box.

Loop: drei Folge-Prüfungen — Cursor auf Sidecar nicht 5K-Gain; Zwei-Pinzette coastet nach Loslassen; 90° Nudge ohne Recalib-Prompt. Kein neues *Need(dt).

# Bugfix-Pass Helios 1.6.53 — 2026-09-08 (Pass 16)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 16 — gelandet

1. **Pointer-Accel × backingScaleFactor.** UV bleibt. `pointerGainScaled` / scale — Retina sonst Teleport.
2. **Two-pinch Scroll-Ticks × backingScale.** 2× = doppelte Ticks, Cap 24.
3. **SpaceMap Rotation-Nudge.** 90/180/270 Palmen halten, HomographieStore.clear. Wipe nur schräg (≥ 15° nicht am Raster).
4. **Click-Hitch Debounce.** `clickHitchNeed` ≥ 0,22 s (8 fps). Freeze-Ende 1 Frame ≠ Klick.

## Offen

DepthCapture Stub. CameraBroker XPC. Overlay-Metal. MediaPipe. VNTrack Hand-Box.

Loop: drei Folge-Prüfungen — Retina-Zeiger nicht teleportiert; 90° Display ohne Recalib-Prompt; Continuity-Miss kein Doppelklick. Kein neues *Need(dt).

# Bugfix-Pass Helios 1.6.52 — 2026-09-08 (Pass 15)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 15 — gelandet

1. **Two-pinch vs Scroll-Hysterese.** Ein Jitter-Frame setzte die Achse auf `.none` → Scale/Scroll-Flip. `twoPinchAxisHysteresis` hält, Flip nur bei 1,35× orthogonal. Kleine Span-Δ = Scroll-Ticks.
2. **SpaceMap Re-Calib nach Display-Drehung.** Homographie nach 90° tot. `spaceMapNeedsRecalib` zirkulär ≥ 15°. Map.rotation gestempelt.
3. **CGEvent coalescing 90 Hz.** Sample-Tick postete 8 Hz und zog Coast zurück. `sampleCursorYieldsToCoast` + `cgEventCoalesceDue` + SuppressionInterval 0.
4. **Sample-Cursor weicht Coast.** Freeze und Drag posten weiter.

## Offen

DepthCapture Stub. CameraBroker XPC. Overlay-Metal. MediaPipe.

Loop: drei Folge-Prüfungen — Zwei-Pinzette jittert nicht zwischen Scroll und Scale; Display drehen → Kalibrier-Hinweis; OS-Cursor coastet ohne 8-Hz-Snap. Kein neues *Need(dt).

# Bugfix-Pass Helios 1.6.51 — 2026-09-08 (Pass 14)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 14 — gelandet

1. **Two-pinch vs Scroll.** `scrollAllowed(..., twoPinch)` + `scrollMuteAfterTwoPinch` 0,28 s. Zoom startete sonst Wisch.
2. **HUD-Coast-Cap × backingScaleFactor.** 80 pt auf Retina zu kurz, Sample warpt. `hudCoastCapScaled`.
3. **Clutch-Jiggle × Scale.** Eigene CGEvent-Echos 2 pt auf 2×. `clutchJiggleScaled`.

## Offen

DepthCapture Stub. CameraBroker XPC. Overlay-Metal. MediaPipe.

Loop: drei Folge-Prüfungen — Zoom ohne Scroll; Coast 2× 160 pt; Retina-Jiggle seizes nicht. Kein neues *Need(dt).

# Bugfix-Pass Helios 1.6.50 — 2026-09-08 (Pass 13)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 13 — gelandet

1. **Clutch-Radius × backingScaleFactor.** 90 Hz Coast postet den OS-Cursor. 48 pt auf Retina 2× zu eng — Hardware-Maus seizes. `clutchOwnRadiusScaled`.
2. **AX-Fenster-ID Cache während Coast.** 90 Hz hitTest + Quant-Key wechselte das Fenster unter dem Zeiger. `axWindowCacheHolds` Bounds+Pad, ID bleibt.
3. **reduced-motion HUD ohne Coast.** `hudCoastAllowed`. Accessibility DisplayLink interpoliert nicht, Sample bleibt.
4. **FramePump Vision-Timeout 400 ms.** VN unkündbar. Stale Frame wird gedroppt, `latest` läuft.
5. **Pinch analog.** Closedness×zSep hält Gate wenn Bool bei 8 fps zittert.

## Offen

DepthCapture Stub. CameraBroker XPC. Overlay-Metal. MediaPipe.

Loop: drei Folge-Prüfungen — Retina-Clutch seizes nicht; Coast hält AX-Fenster; Reduce-Motion HUD steht. Kein neues *Need(dt).

# Bugfix-Pass Helios 1.6.49 — 2026-09-08 (Pass 12)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 12 — gelandet

1. **720p-Lock persist.** `helios.formatHeight`. Continuity startet 720, nicht claimed 1080@30. Mac nicht auf Phone-720 kleben.
2. **FPS-Lock 24.** Continuity 30-Request fiel auf 8. `cameraLockDuration` 1/24.
3. **Click-vs-Drag Palm-Vel.** Slow-Drift kein Zug. Flick ist Drag trotz kurzer Distanz.
4. **HUD-Coast 90 Hz.** DisplayLink extrapoliert, OS-Cursor folgt. Clutch Radius statt Zeitfenster.
5. **Per-Display SpaceMap.** Homographie vom Screen unter dem Cursor.
6. **Body-Pose skip bei 8 fps.** Continuity spart den Extra-Vision-Tick.

## Offen

DepthCapture Stub. CameraBroker XPC. Overlay-Metal. MediaPipe.

Loop: drei Folge-Prüfungen — Continuity 720p@24 nach Kaltstart; Slow-Pinch = Klick; HUD-Cursor coastet zwischen Ticks. Kein neues *Need(dt).

# Bugfix-Pass Helios 1.6.48 — 2026-09-08 (Pass 11)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 11 — gelandet

1. **sessionPreset nicht 1080p.** `.hd1920x1080` klemmte Continuity auf 8 fps, Leiter war tot. `inputPriority` → 720p.
2. **Native 420 vor BGRA.** Continuity konvertierte sonst jeden Frame nach 32BGRA.
3. **Freeze treibt Cursor.** Kalman-Palme + `lastHandsLive.shifted` — Hitch ließ den Zeiger stehen.
4. **Freeze hält Pinch und AX-Zug.** `emptyHandsHoldDropsPinch` / `ReleaseAX` nur jenseits Hold. Continuity-Miss war Klick + Fenster-Drop.
5. **Sleep/Wake Session-Recovery.** `didWake` → `start()`, Leiter bleibt.

## Offen

DepthCapture Stub. Zwei TCC mit Aegis (CameraBroker). Overlay-Metal.

Loop: drei Folge-Prüfungen — Continuity 720p@24 nach Start; Freeze bewegt Cursor; Pinch überlebt Hitch. Kein neues *Need(dt).

# Bugfix-Pass Helios 1.6.47 — 2026-09-08 (Pass 10b)

Kein Merge von `bugfix`. `[skip ci]`.

## Pass 10b — gelandet

1. **start() hält Leiter.** 1.6.46 setzte lastFormatHeight=1080 und lastDeviceUniqueID="" — Continuity-Reconnect kletterte wieder 1080p@8.
2. **lastFormatHeightResets sticky.** uniqueID-Wechsel allein dumpte Höhe. Jetzt Name+Role, Mac nie.
3. **Mac nie sticky.** FaceTime-Name ist nicht Continuity.

## Offen

DepthCapture Stub. Zwei TCC mit Aegis (CameraBroker). Sleep/Wake Camera-Recovery.

Loop: drei Folge-Prüfungen — sticky iPhone hält 720; Mac-ID reset; start() ohne 1080-Wipe. Kein neues *Need(dt).

# Bugfix-Pass Helios 1.6.46 — 2026-09-08 (Pass 10)


Kein Merge von `bugfix`. `[skip ci]`.

## Pass 10 — gelandet

1. **uniqueID sticky.** Continuity flackert uniqueID, Homographie+Recenter bei jedem Reconnect. `cameraIDSticky` / `cameraIDHomographyResets`: gleicher Name (ohne ` · Tiefe`) hält Map. `SpaceMap.retarget` schreibt Homographie unter neue ID. Erster Frame (prev leer) kein Reset.
2. **PTS-Sprung = Freeze.** `accept()` nahm Roh-PTS als `arrived`. Continuity-Uhr-Reset = multi-second Dropout, Hands tot. `ptsJumpIsFreeze` (d ≤ 0 oder > 0,50 s) + `ptsWallStamp` auf Wall.
3. **lastFormatHeight Reset.** Leiter klebte 1080p der alten Cam nach uniqueID-Wechsel. `lastFormatHeightResets` setzt Höhe auf 1080.
4. **preferredDevice Name-Reconnect.** uniqueID tot nach Reconnect → Built-in. `cameraPreferredID` matcht ID, sonst `cameraNameBare`. `helios.cameraName` persistiert.
5. **Kalman-Palme in HandTracker während Freeze.** Leere Observations standen 90 ms. `freezeKalmanPredict` mit `lastVel` (Palm-Delta live) schiebt lastHands.

## Offen

DepthCapture Stub. Zwei TCC mit Aegis (CameraBroker). Sleep/Wake Camera-Recovery.

Loop: drei Folge-Prüfungen — Continuity Reconnect ohne Recenter; PTS-Sprung hält Palm; Leiter nach Cam-Wechsel neu.
