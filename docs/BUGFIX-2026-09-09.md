# Bugfix-Pass Helios 1.6.87 — 2026-09-09 (Pass 36)

Kein Merge von `bugfix`. `[skip ci]`. Predict bleibt 0 (1.6.66). Overlay 1.6.68/69 unangetastet. Kein neues *Need(dt).

## Warum es schlecht funktionierte

1.6.84 verdrahtete `pinchHoldsGrab`. `driveGrab` ORte `analogClosed` auf `holdOk`. analog ≥ 0,58 (Faust) hielt den Zug, obwohl `pinchHoldsGrab` ohne Reach/allowFist false war. analogClosed ist Gate für Start/Hold (Vision-Jitter 8 Hz), nicht Hold selbst.

## Pass 36 — gelandet

1. **`pinchHoldOk(hold:analogClosed:beak:)`.** analogClosed ignoriert. Hold oder Schnabel.
2. **`driveGrab` Hold = pinchHoldOk.** analogClosed bleibt Gate in pinchStartsGrab/pinchHoldsGrab.

Nicht angefasst: Predict-Cap (0), Overlay, CameraBroker, MediaPipe, VNTrack. analogClosed als Start-Gate bleibt. Relativer Zeiger/One-Euro aus 1.6.86 unangetastet.

## Offen

CameraBroker XPC. Overlay-Metal. MediaPipe. VNTrackObjectRequest. DepthCapture LiDAR. Mission-Control Spread. Continuity 15-fps Probe. analogClosed Start härter. Palm-UV Highpass. HUD-Coast.

Loop: drei Folge-Prüfungen — Faust analog 0,83 gibt Zug frei; echte Pinzette hält; Schnabel-Zug hält. Kein neues *Need(dt).

# Bugfix-Pass Helios 1.6.86 — 2026-09-09 (Pass 35)


Kein Merge von `bugfix`. `[skip ci]`. Predict bleibt 0 (1.6.66). Overlay 1.6.68/69 unangetastet. Kein neues *Need(dt).

## Warum es schlecht funktionierte

Nicht fehlende Schwellen. `pointerGain`, `pointerGainDt`, `pointerGainAdaptive`, `oneEuroFilter`, `palmDeadmanClutch`, `deadzone2D`, `inCornerRest`, `pinchTapWouldClick`, `twoPinchAxisHolds`, `pinchFollowID` existieren — **keine Call-Site in `mappedPoint` / `driveGrab`**.

`mappedPoint` nahm 58–93 % des *absoluten* SpaceMap-Samples. 8-Hz-Continuity = Teleport. Der Gain-Slider schrieb `engine.pointerGain` und blieb tot. `euroInited` / `palmJitter` / `palmDeadman` waren State ohne Tick. Recenter löschte `cursorSmooth` → Sprung auf Palmen-UV.

`isClick` allein ohne Sequenz: Meter-zu → auf kann klicken ohne zu→auf-Trail.

`twoHandClutchOn` wurde in `placeCursors` gesetzt — **mappedPoint las es nie**. Zweite Palme stiehlt den Actor-Delta. `clickHitchFromFreeze` tot: `fireTapClick` ruft `click(force: true)` und umgeht hitch. Nach Freeze-Ende klickt der erste Jitter.

## Pass 35 — gelandet

1. **Relativer Zeiger.** Delta × Gain × Dt × Adaptive. Recenter = Trackpad-Clutch (Abs-Tracks leer, Cursor bleibt).
2. **One-Euro live** auf dem Actor. Totzone, Ecken-Rest, 2 s Deadman STILL.
3. **pinchTapWouldClick** neben isClick. Closedness-Trail 8 Samples.
4. **twoPinchAxisHolds** vor Edge-Hold. **pinchFollowID** in pinchActor.
5. **twoHandClutchOn blockt Delta** (HUD 2HAND). Zweite Palme ohne Zwei-Pinch clutcht.
6. **clickHitchFromFreeze** in fireTapClick und driveRightClick. force:true umgeht hitch nicht mehr nach Freeze.

Nicht angefasst: Predict-Cap (0), Overlay, CameraBroker, MediaPipe, VNTrack.

## Offen

CameraBroker XPC. Overlay-Metal. MediaPipe. VNTrackObjectRequest. DepthCapture LiDAR. Mission-Control Spread. Continuity 15-fps Probe. Per-Finger-Kontakt. palmHighpass, hudCoastPoint, flingFromTrail, chiralityLock.

Loop: drei Folge-Prüfungen — Gain-Slider bewegt den Cursor; Recenter springt nicht; 8 Hz ohne Teleport-Klick; zweite Palme kein Steal; Freeze kein Sofort-Klick. Kein neues *Need(dt).

# Bugfix-Pass Helios 1.6.85 — 2026-09-09 (Pass 34)

Kein Merge von `bugfix`. `[skip ci]`. Predict bleibt 0 (1.6.66). Overlay 1.6.68/69 unangetastet. Kein neues *Need(dt).

## Warum es schlecht funktionierte

Nicht fehlende Schwellen. `isClick`, `chromeDwellFires`, `twoPinchScrollCoastTicks`, `driveRightClick`, `fireChrome` existieren — **keine Call-Site**. `driveGrab` rief `fireTapClick` bei jedem Pinch-Release. Continuity 8 Hz schließt Finger im Rauschen: ein Tick zu → auf = Desktop-Klick.

`magnetChrome` schrieb nur das HUD-Label. Loslassen klickte den Pixel unter dem Cursor, nie Schließen/Minimieren.

`driveRightClick` tot. Ring+Pinzette wurde Ziehen.

Empty-Hands setzte `twoPinchScrollStreak = 0` **vor** Coast. `handleTwoPinchScale` coaste `twoPinchLastTicks` auch nach Jitter-Streak 1.

## Pass 34 — gelandet

1. **`isClick` vor `fireTapClick`.** Halt, Palm-HW, Cursor-px, Energy, dt. Drift/Jitter kein Klick.
2. **Ampel: `chromeHotKnob` + `chromeDwellFires`.** Volle Dwell → `fireChrome`. Unvollständig → kein Desktop-Durchklick.
3. **`driveRightClick` vor `driveGrab`.** Zählt 0,32 s, blockt Grab, feuert Rechtsklick.
4. **`twoPinchScrollCoastTicks`.** Coast nur nach Edge-Streak. Jitter tot.

Nicht angefasst: Predict-Cap (0), Overlay, CameraBroker, MediaPipe, VNTrack, pinchTapWouldClick (Sequenz bleibt Tests).

## Offen

CameraBroker XPC. Overlay-Metal. MediaPipe. VNTrackObjectRequest. DepthCapture LiDAR. Mission-Control Spread. Continuity 15-fps Probe. pinchTapWouldClick live.

Loop: drei Folge-Prüfungen — ruhende Palme kein Klick; Ampel-Loslassen schließt; Mini-Zwei-Pinch coaste nicht. Kein neues *Need(dt).

# Bugfix-Pass Helios 1.6.84 — 2026-09-09 (Pass 33)

Kein Merge von `bugfix`. `[skip ci]`. Predict bleibt 0 (1.6.66). Overlay 1.6.68/69 unangetastet. Kein neues *Need(dt).

## Warum es schlecht funktionierte

Nicht fehlende Schwellen. `pinchStartsGrab` / `pinchHoldsGrab` existieren seit 1.6.67, Tests grün — **keine Call-Site in `driveGrab`**. Der Tick nahm `pinchMeterClosed` (`closedness ≥ 0,24` oder Vision-Gate). Continuity 8 Hz schließt Finger im Rauschen. Pose-Rest (`openPalm` / `thumbsUp`) greift nur wenn der Klassierer trifft. Faust mit geschlossenen Spitzen war Tap.

Stamp-Zeile ohne TTL: Aegis las Palmen 12 s nach Helios-Crash und skippte echte Gesichter.

1.6.73 Release bei Interrupt. Heartbeat 80 ms schrieb Stamp+Claim sofort wieder — tote Palmen-UV und Continuity-Claim, während die Session tot war.

## Pass 33 — gelandet

1. **`driveGrab` Start = `pinchStartsGrab`.** Reach/Zeigefinger oder Schnabel. Meter 0,24 allein startet nicht.
2. **Hold = `pinchHoldsGrab`.** Faust nur während Zug/Schnabel. Analog ≥ 0,58 hält über einen Reach-Jitter.
3. **Idle-Arm `pinchStartsGrab`.** Jitter schärft nicht; Timeout `idleHandArm` bleibt.
4. **`cameraMutexStampFresh` 250 ms.** Feld 2 Unix. Media-PTS tot.
5. **Interrupt: Beat tot.** `cameraMutexBeatAllowed`. Release löscht Palmen. Kein Re-Claim 80 ms später.

Nicht angefasst: Predict-Cap (0), Overlay, CameraBroker, MediaPipe, VNTrack.

## Offen

CameraBroker XPC. Overlay-Metal. MediaPipe. VNTrackObjectRequest. DepthCapture LiDAR. Mission-Control Spread. Continuity 15-fps Probe.

Loop: drei Folge-Prüfungen — ruhende Palme kein Klick; echte Pinzette klickt; Stamp 400 ms tot in Aegis; Interrupt kein MUTEX-helios. Kein neues *Need(dt).
